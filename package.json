{
  "name": "openclaw-stack",
  "type": "module",
  "private": true,
  "scripts": {
    "pre-deploy": "node build/pre-deploy.mjs",
    "pre-deploy:dry": "node build/pre-deploy.mjs --dry-run",
    "deploy-report": "node build/generate-deploy-report.mjs",
    "deploy-report:save": "node build/generate-deploy-report.mjs --save --print",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "handlebars": "^4.7.8",
    "js-yaml": "^4.1.0",
    "jsonc-parser": "^3.3.1"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
